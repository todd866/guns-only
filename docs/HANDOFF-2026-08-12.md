# Guns Only handoff — 2026-08-12 (BF:V push, stopped pre-ship of Build 313)

Owner stopped the session to conserve tokens: "commit and push what you've got … record
what's next and how to build it." Read this, `docs/STATUS.md`, and the two specs below
before continuing.

## Where things stand

- **Production is Build 312** (verify at `https://guns-only.com/api/build-info`): the crash
  envelope. Botched flying ends in named crashes (HARD IMPACT / ROLLOVER / SPIN CONTACT /
  ROTOR STRIKE / INTO THE RIVER), gear-damage tier, touchdown telemetry, honest
  autorotation (frozen pilot dies, flown flare survives), de-glowed laterite road.
- **Build 313 is COMPLETE ON THE BRANCH but NOT shipped**: `feature/camp-ember-firebase`
  (pushed). Camp Ember rebuilt to the NARA photo dossier (berm ring, rosettes, scar fan,
  tracks, bunker mounds, dead-tree accents), three Cobras on the ramp (two parked
  silhouettes via `cobra_parked_airframe.js`), automatic bird swap on crippled stable pad
  contact, FOB COMBAT INEFFECTIVE pool terminal, flicker probe
  (`tools/cobra-scenery-gate/flicker.mjs`) + polygonOffset decal bias.
  All suites green at stop: 13 mission-runtime (3 new pool tests), 38 dynamics, 200 cobra
  render, firebase 9/9. STATUS.md already reconciled for the 313 candidate. NOT stamped.
- **Doctrine recorded this session**: "if in doubt, we're trying to clone BF:V" (memory
  `bfv-clone-tiebreaker`); the owner resequenced FOB work before threat work.

## To ship Build 313 (next session, in order)

1. **Adjudicate the reviews.** Cursor's full review is preserved at
   `docs/work-orders/2026-08-12-build-313-cursor-review.log`. Its four real findings, all
   non-blocking polish (fix items 2-4 before stamping; item 1 is a design call):
   1. *Terminal precedence*: a crippled-AND-unflyable bird on the pad with an empty pool
      yields VehicleAuthorityLost, not FobCombatIneffective — decide which should win
      (probably FOB INEFFECTIVE when `IsInsideFob`), then encode in a test.
   2. *Restart hygiene*: `restartRoute` in `web/wwwroot/cobra-lab/main.js` must clear
      `parkedPresences` (remove groups from scene) and reset `lastAirframeSwaps`.
   3. *Crippled parked pose*: snap the crippled slot's stored Y to terrain+skid at its XZ
      (it currently stores live CoM; off the flat apron the wreck floats/clips, and the
      list rotation exaggerates it).
   4. *Spare-station overlap*: if the player stabilizes ON a Ready station, the wreck pose
      and the spawned spare overlap (presentation-only, no sim collision). Cheapest fix:
      reject swap while within ~8 m of the target spare's station, or offset the wreck.
   Also add Cursor's "missing tests": R clears parked presences; post-swap Act/pose
   coherence; the item-1 terminal.
   A Codex review was still running at stop (`scratchpad/codex-review-313.log` in the
   session dir, if it survives). If absent, rerun: `~/.claude/bin/codex-adversary.sh
   --mode diff --repo <worktree> --base origin/main --effort xhigh` with an explicit
   no-dotnet warning in `--focus` (its sandbox has no .NET 8).
2. **Stamp LAST**: `bin/stamp-release --next 313`, then commit (STATUS is already
   reconciled — re-check it still tells the truth after item 1's decision).
3. **Full gate** in the worktree (`.worktrees/cobra-hud-waterline-launchpad`):
   `PATH="$HOME/.nvm/versions/node/v24.18.1/bin:/opt/homebrew/bin:$PATH"
   GUNS_DOTNET_CLI="$HOME/.dotnet/dotnet" DOTNET_ROOT="$HOME/.dotnet"
   DOTNET_MULTILEVEL_LOOKUP=0 ./bin/check` — final line must read "Guns Only checks
   passed". (Node 24 is REQUIRED; memory `ship-ritual-2026-08-11` has every gotcha.)
4. **PR → CI → merge**: PRs #61/#62 are the template. `boot does not stutter` may flake
   once; one re-run is precedent.
5. **Deploy**: `.worktrees/deploy-266` is on main, hydrated, `web/smoke` deps installed —
   `git pull --ff-only && ./bin/worktree-prep "$PWD"` then `./bin/deploy-web --prod`;
   verify build-info reports 313. Deploy authorization: the owner's standing "get it
   built" covered this push; re-confirm only if scope changed.
6. **Owner flight is the flicker gate.** The static probe reads 0.00-0.29% (clean); the
   reported shimmer is motion/GPU-dependent and SwiftShader probes CANNOT prove it fixed.
   polygonOffset + respaced layers are the physical mitigations aboard 313. Ask the owner
   WHAT flickers (ground surfaces / shadows / everything) — that answer selects between
   the remaining suspects: mm-coplanar terrain decals elsewhere (road ribbons, water
   seams) vs Build 308's cast-shadow shimmer (bias/frustum audit).

## Build 314: the battlefield fights back (designed, not started)

Spec Build B of `docs/superpowers/specs/2026-08-12-cobra-battlefield-fights-back-design.md`
(owner-approved): DShK 12.7 + ZPU 14.5 sites + small-arms harassment as new `GroundWar`
roles; sim-owned deterministic fire (LOS through the ONE terrain sampler, range/exposure
envelopes, travel time + dispersion, burst-geometry hits on fuselage/tail capsules — never
hitscan); tracers/muzzle-flash as snapshot events (tracers ARE the telegraph); subsystem
damage degrading real `Ah1gCobraDynamics` inputs (engine power ×, tail-rotor authority ×
— feeds the torque-yaw machinery → pedal work → spin → the autorotation Build 312 made
survivable —, SCAS-out, fuel-mass leak); radio call on first engagement; NO MANPADS.
Gates with control experiments: exposed hover ~400 ft/600 m → hit in 10-25 s; masked low
ingress → zero hits; tail hit → measured pedal-workload increase; determinism. Damaged
birds then feed the 313 swap loop — the full BF:V loop closes. Write the implementation
plan off the 312/313 plan pattern (`docs/superpowers/plans/`), TDD with the
`Ah1gCobraDynamicsTests`/`CobraMissionRuntimeTests` idioms.

## After 314 (the standing roadmap)

Mission director (patrols/ambushes/convoys) → corridor scenery at BF:V density (crater
sticks + defoliation swathes per the dossier; the TRUE terrain-material laterite scar
replaces 313's fan; the road treatment) → crewed co-op on the Build 310 arena infra →
stylized infantry. Each gets its own brainstorm round with the owner.

## Verification doctrine (unchanged, non-negotiable)

Rendered-frame QA before shipping visual work; live-page QA via
`window.__gunsOnlyCobraAuthority` + synthetic KeyboardEvents; adversarial review with the
test files IN the Cursor bundle and a no-dotnet warning to Codex; a control experiment on
every new gate (this session deleted a fix+test pair that couldn't fail — that discipline
is why the suites can be trusted); stamp last after a truthful STATUS reconcile.
